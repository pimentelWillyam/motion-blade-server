import { ServantSorter } from "./bot/helper/ServantSorter"


const debug = async (): Promise<void> => {
  const servantSorter = new ServantSorter()
  
  const res = sorter.sort([1, 7, 2, 4, 3, 5, 6, 9, 8], 'quicksort')
}

void debug()
